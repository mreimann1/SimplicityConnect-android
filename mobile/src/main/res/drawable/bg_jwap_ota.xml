<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background -->
    <solid android:color="@color/jwap_ota_background" />

    <!-- Outline / stroke -->
    <stroke
        android:width="1dp"
        android:color="@color/jwap_ota_stroke" />

    <!-- Optional: rounded corners (remove if undesired) -->
    <corners android:radius="4dp" />

</shape>